    DELETE TV_TDAILY_WELL_TNX;
    INSERT INTO TV_TDAILY_WELL_TNX
    SELECT * FROM V_TDAILY_WELL_TNX;
    COMMIT;
    DELETE TV_TWELL_SPECS;
    INSERT INTO TV_TWELL_SPECS
    SELECT * FROM V_TWELL_SPECS;
    COMMIT;
    DELETE TV_TDAILY_WELL_INJ_TNX;
    INSERT INTO TV_TDAILY_WELL_INJ_TNX
    SELECT * FROM V_TDAILY_WELL_INJ_TNX;
    COMMIT;
/////////////////////////UPDATE
DELETE TV_TDAILY_WELL_TNX
WHERE PRODUCTION_DATE > SYSDATE - 30;
INSERT INTO TV_TDAILY_WELL_TNX
SELECT * FROM V_TDAILY_WELL_TNX
WHERE PRODUCTION_DATE > SYSDATE - 30;
COMMIT;
DELETE TV_TWELL_SPECS
WHERE PRODUCTION_DATE > SYSDATE - 30;
INSERT INTO TV_TWELL_SPECS
SELECT * FROM V_TWELL_SPECS
WHERE PRODUCTION_DATE > SYSDATE - 30;
COMMIT;
DELETE TV_TDAILY_WELL_INJ_TNX
WHERE PRODUCTION_DATE > SYSDATE - 30;
INSERT INTO TV_TDAILY_WELL_INJ_TNX
SELECT * FROM V_TDAILY_WELL_INJ_TNX
WHERE PRODUCTION_DATE > SYSDATE - 30;
COMMIT;